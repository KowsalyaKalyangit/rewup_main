 

import '../allpackages.dart';

class Routes {
  static final routes = [
    // GetPage(name: '/', page: () => SplashScreen()),

    GetPage(name: '/home', page: () => const HomeScreen()),
     
  ];
}
